export interface ConvertedLibrary {
    // Define the properties and methods of the converted library here
}

export interface Helpers {
    // Define the properties and methods of the helper functions here
}